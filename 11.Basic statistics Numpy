Data Analytics:
  Summarize large data

statistics:
Mean:
np.mean(np_city[:0])  =>avaerage 

Median:
np.median(np_city[:,0])

SD:
np.std(np_city[:,0])

corralated:
np.corrcoef(np_city[:,0],np_city[:,1])

to form a array of two coln:
np_a=np.column_stack(coln1,coln2)


Generate data:
using randaom distribution

np.random.normal(dist mean, dist std, number of data)
